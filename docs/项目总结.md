# WutheringWavesDmgCalc 项目总结

> 开发起始: 2026-05-02 | 最后更新: 2026-06-12 | 版本: v1.0（正式版）

---


> 📌 **历史存档**：docs/PROJECT_SUMMARY.md.bak 保留了旧版完整的技术文档（含13条数据流、29个核心类、主题系统等详细内容），如遇到当前文档未覆盖的疑难问题，可查阅此备份文件对照修复。
## 一、项目概览

**WutheringWavesDmgCalc** 是《鸣潮》（Wuthering Waves）的伤害计算器桌面应用，基于 PyQt6 开发。项目从 2026 年 5 月 7 日开始构思，5 月 12 日正式动工，6 月 12 日发布 v1.0 正式版，历时约一个月。

### 功能总览

- **完整伤害计算引擎** — 9 大乘区（基础/加成/加深/暴击/倍率/防御/抗性/独立乘区/全伤害），damage_calc.py 为唯一真相来源
- **OCR 图文识别** — 支持声骸属性截图识别、技能倍率截图识别（PP-OCRv5 + ONNX Runtime）
- **预设系统** — 角色/武器/声骸套装/角色增益四类预设，含独立计算引擎和关键词关联
- **官方预设同步** — 从 GitHub 一键拉取最新预设，多源 fallback（直连 + 镜像 + CDN），国内无需 VPN
- **暗色/亮色双主题** — 全界面自适应配色
- **存档系统** — 完整 JSON 存档，支持保存/加载/快速保存
- **错误自动报告** — 崩溃时自动启动 ErrorViewer 弹窗，错误日志持久化
- **自动化测试** — pytest 186 个用例，2.9 秒跑完
- **Git 版本管理** — 内置版本快照，可随时回退
- **辅助工具** — Git 代理管理工具 + 官方预设上传工具（均已打包为独立 exe）

### 技术栈

| 类别 | 技术 |
|------|------|
| 语言 | Python 3.13 |
| GUI 框架 | PyQt6 |
| OCR | OpenCV + ONNX Runtime + PP-OCRv5 |
| 图表 | matplotlib |
| 测试 | pytest |
| 打包 | PyInstaller 6.20 |
| 版本管理 | Git |
| 代码量 | 主编 ~10862 行 + 7 个拆分模块，总计约 18000+ 行 |

---

## 二、UI 架构

### 主窗口布局

```
QMainWindow
├── QStackedWidget
│   ├── WelcomeScreen              ← 欢迎页（首次启动、版本提示）
│   └── MainScreen                 ← 主界面
│       ├── 侧边栏导航树（5 分类，~14 页面）
│       └── QStackedWidget 页面区
│           ├── CharBasePage       ← 角色武器基础值
│           ├── CombinedEntryPage  ← 综合填写
│           ├── EnemyDefensePage   ← 敌方防御
│           ├── EnemyResistancePage← 敌方抗性
│           ├── IndepZonePage      ← 独立乘区
│           ├── EchoPage           ← 声骸编辑/OCR
│           ├── ResultPage         ← 倍率/计算结果
│           ├── ResultListPage     ← 结果列表/对比
│           ├── SummaryPages       ← 数值汇总（3 页）
│           ├── ResonanceBuffPage  ← 共鸣链增益
│           └── KeywordAssociationPage ← 关键词关联
```

### 导航树（5 分类）

| 分类 | 页面 | 说明 |
|------|------|------|
| 🎭 角色基础 | 角色武器 / 基础数值覆盖 | 输入角色武器基础属性 |
| 📊 数值填写 | 综合填写 / 共鸣链增益 / 关键词关联 | 常驻 & 触发效果条目管理 |
| 👻 声骸 | 声骸编辑 / OCR 导入 | 声骸属性输入和截图识别 |
| 🛡 敌方信息 | 防御 / 抗性 | 敌人等级、抗性设置 |
| 📈 计算结果 | 结果页 / 结果列表 / 数值汇总（×3） | 伤害计算、对比、汇总 |

---

## 三、数据流

### 核心公式

```
最终伤害 = 基础乘区 × 加成乘区 × 加深乘区 × 暴击乘区 × 倍率乘区
          × 防御减伤 × 抗性乘区 × 独立乘区 × 全伤害加成
```

### 数据链路（12 条）

1. **角色基础值 → 最终伤害** — 基础攻击/生命/防御注入计算公式
2. **综合填写（常驻+触发）→ 最终伤害** — 效果条目按乘区分类路由
3. **声骸 → 最终伤害** — 主/固/副词条按名称分类到各乘区
4. **共鸣链增益 → 综合填写 + 关键词关联** — 6 链卡片式管理，启用联动
5. **手动关键词关联 → 最终伤害** — 关键词交集匹配，精确控制生效范围
6. **敌人防御减伤 → 最终伤害** — 等级差公式（含无视防御、截断逻辑）
7. **敌人抗性 → 最终伤害** — 正负抗性 clamp 处理
8. **独立乘区 → 最终伤害** — 组内加法 × 组间乘法
9. **ResultPage → 计算结果 → 结果列表** — 多方案对比
10. **上游任意变更 → 全局回调链** — 300ms 防抖自动重算
11. **总结页反向编辑 → 上游同步** — 副名称双向同步
12. **存档保存/加载** — 完整状态序列化

---

## 四、OCR 系统

- **模型**：PP-OCRv5（det + rec），ONNX Runtime 推理
- **识别能力**：声骸面板截图 → 自动裁切 → 识别主词条/副词条/套装名
- **技能倍率识别**：截图识别倍率数值，自动填入 ResultPage
- **字符修正**：内置鸣潮专有名词词库，提高生僻字识别率

---

## 五、预设系统

| 类别 | 说明 |
|------|------|
| 角色预设 | 基础属性 + 倍率 + 共鸣链效果 + 独立乘区 + 结果列表 |
| 武器预设 | 基础攻击 + 附加属性 + 精炼效果 |
| 声骸套装预设 | 套装阶段效果 + 首位声骸增益 |
| 角色增益预设 | 常驻 + 触发效果 + 关键词关联 |

所有预设支持：创建/编辑/保存/加载/搜索/预览/应用。

---

## 六、辅助工具

项目 `tools/` 目录下提供两个独立工具，均已打包为 exe：

| 工具 | 路径 | 功能 |
|------|------|------|
| **Git 代理管理** | `tools/Git代理管理/GitProxyManager.exe` | 自动扫描代理端口，一键配置 git 代理 |
| **上传官方预设** | `tools/上传官方预设/PresetUploader.exe` | GUI 界面上传 presets/official/ 到 GitHub |

---

## 七、打包规则

使用 PyInstaller 打包，打包入口为 `packaging/WWDmgCalc.spec`，后处理脚本为 `packaging/after_build.py`。

### 最终发布结构

```
dist/WutheringWavesDmgCalc/          ← 主文件夹
├── WWDmgCalc.lnk                    ← 快捷方式 → WWDmgCalc/WWDmgCalc.exe
├── ErrorViewer.lnk                  ← 快捷方式 → ErrorViewer/ErrorViewer.exe
├── WWDmgCalc/                       ← 主程序
│   ├── WWDmgCalc.exe
│   └── _internal/                   ← 依赖库（PyQt6 / cv2 / numpy / onnxruntime / PIL ...）
│       ├── manual/                  ← 使用手册
│       ├── error_handler/           ← 错误处理模块
│       ├── models/                  ← OCR 模型文件
│       └── damage_calc.py           ← 计算引擎
├── ErrorViewer/                     ← 错误报告程序
│   ├── ErrorViewer.exe
│   └── _internal/
├── tools/                           ← 辅助工具
│   ├── Git代理管理.lnk              ← 快捷方式 → GitProxyManager.exe
│   ├── 上传官方预设.lnk             ← 快捷方式 → PresetUploader.exe
│   ├── Git代理管理/                 ← Git 代理管理工具（独立 exe）
│   │   ├── GitProxyManager.exe
│   │   └── _internal/
│   └── 上传官方预设/                ← 预设上传工具（独立 exe）
│       ├── PresetUploader.exe
│       └── _internal/
├── config/
│   └── auto_all_config.json         ← 用户配置
├── presets/
│   ├── official/                    ← 官方预设（含 JSON 文件 + manifest.json）
│   │   ├── character/
│   │   ├── weapon/
│   │   ├── echo_set/
│   │   └── character_buff/
│   └── user/                        ← 用户预设（仅空目录，运行时创建）
│       ├── character/
│       ├── weapon/
│       ├── echo_set/
│       └── character_buff/
└── save/                            ← 存档目录（空目录，运行时创建）
```

### 打包步骤

```bash
# 1. 安装依赖
pip install pyinstaller

# 2. 打包主程序
cd packaging
python -m PyInstaller WWDmgCalc.spec --clean

# 3. 打包辅助工具（单独 spec）
cd tools_specs
python -m PyInstaller git_proxy_manager.spec --distpath ../dist_tools --workpath ../build_tools --clean
python -m PyInstaller preset_uploader.spec --distpath ../dist_tools --workpath ../build_tools --clean

# 4. 重组结构
cd ..
python after_build.py
```

### 路径说明（打包后）

| 路径类型 | 取值 | 说明 |
|----------|------|------|
| 用户数据 | `_APP_DIR` = exe 的父级父级 | 存 save/ config/ presets/ |
| 只读数据 | `_DATA_DIR` = `sys._MEIPASS`（即 _internal/） | 存 manual/ error_handler/ models/ |
| 预设同步 | `presets/official/` | 从 GitHub 拉取更新，位于主文件夹下 |

---

## 八、GitHub 集成

| 功能 | 状态 | 说明 |
|------|------|------|
| 源码仓库 | ✅ 已发布 | [github.com/JadeYingWah/WutheringWavesDmgCalc](https://github.com/JadeYingWah/WutheringWavesDmgCalc) |
| Release 发布 | ✅ v0.15 → v1.0 | 含编译好的 exe 包，用户直接下载解压使用 |
| 官方预设同步 | ✅ 已实现 | 在预设加载器点击「更新官方预设」从 GitHub 拉取 |
| 预设上传工具 | ✅ 已实现 | `tools/上传官方预设/PresetUploader.exe`，GUI 界面一键上传 |
| 同步 fallback | ✅ 多源自动重试 | raw.githubusercontent.com → raw.gitmirror.com → cdn.jsdelivr.net |

---

## 九、开发注意事项

### 错误处理
- 所有弹窗统一 `_center_window()` 居中屏幕
- 崩溃捕获自动写入 error_log.json + 启动 ErrorViewer
- 预设同步失败写入错误日志，侧边栏可查看

### 退出保存
- 关闭窗口时弹出「保存并退出/不保存/取消」对话框
- X 按钮 = 取消（隐藏 Cancel 按钮绑定关闭事件）

### 文件名规范
- 存档名和预设名禁止包含 `\/:*?"<>|` 等非法字符
- 输入时弹窗提示，不静默删除

---

## 十、版本历史

| 版本 | 日期 | 主要内容 |
|------|------|----------|
| v0.9 | 06-09 | 初始开发，基础伤害计算框架 |
| v0.12 | 06-11 | 预设系统乘区化改造、使用预设重设计 |
| v0.14 | 06-11 | 角色增益预设、分页式编辑、OCR 分类 |
| v0.15 | 06-12 | OCR PP-OCRv5 升级、打包重组、预设同步 |
| v1.0 | 06-12 | 正式版：工具打包、Git 代理管理、预设上传工具 |
