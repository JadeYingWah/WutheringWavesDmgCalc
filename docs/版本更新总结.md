# WutheringWavesDmgCalc 版本更新总结

> 开发起始: 2026-05-02 | 最后更新: 2026-06-15 | 版本: v1.0（正式版）

---


## v1.0 正式版 — 最终完善

- **v1.0**：打包规则修正 — save/ 仅空目录、presets/user/ 仅空子目录，不打包开发机测试数据（2026-06-13）
- **v1.0**：GitHub Actions 自动打包工作流 — 手动触发，自动下载 OCR 模型、打包主程序 + 工具、重组结构、创建 Release
- **v1.0**：feat：预设作者系统 — 四种预设窗口新增「作者」输入框，JSON 存储 author 字段，预设详情显示作者名（2026-06-13）
- **v1.0**：feat：贡献者名单 — 欢迎页「🎖️ 贡献者名单」按钮，弹窗扫描所有预设 author 字段去重展示（2026-06-13）
- **v1.0**：打包 spec 修复 — 工具 spec 移入 packaging/ 目录，排除项完善减体积，图标路径统一为 ../ico/icon.ico
- **v1.0**：使用手册图片路径修复 — content.json 中 file:/// 绝对路径改为 manual/images/ 相对路径
- **v1.0**：.gitignore 完善 — 补全 tools/preset_uploader/.upload_token 忽略
- **v1.0**：项目总检查通过 — 路径、图标、打包、漏洞、安全全量检查

- **v1.0**：修正打包结构——工具快捷方式放在 tools/ 文件夹内，主文件夹只保留 WWDmgCalc.lnk 和 ErrorViewer.lnk
- **v1.0**：项目文档全面重写——含最终发布结构图、打包步骤、路径说明
- **v1.0**：版本更新总结整理——按版本倒序排列，完整记录所有改动
- **v1.0**：正式版发布——从 2026.05.02 到 06.12，历时 42 天

## v0.15 — 打包完善与预设同步

- **v0.15**：打包路径修复——`_APP_DIR` 在 frozen 模式指向主文件夹，`_DATA_DIR` 指向 `_MEIPASS`，使用手册/存档/预设路径统一
- **v0.15**：文件名非法字符校验——存档命名和预设命名弹窗提示 `\/:*?"<>|` 不可用
- **v0.15**：退出弹窗修复——隐藏 Cancel 按钮实现 X 关闭功能
- **v0.15**：after_build.py 完善——presets/official/ 复制文件，presets/user/ 和 save/ 只建空目录，清理误打包的 WWDmgCalc/presets/
- **v0.15**：预设同步多源 fallback——raw.githubusercontent.com + raw.gitmirror.com + cdn.jsdelivr.net 自动重试，国内无需 VPN
- **v0.15**：预设同步错误日志——下载失败详情写入 error_log.json
- **v0.15**：同步进度窗口改进——550×550 尺寸、文字自动换行、分段进度显示
- **v0.15**：同步中文 URL 编码修复——urllib 不支持中文文件名，用 quote() 转义
- **v0.15**：ErrorViewer 日志路径修复——打包后正确指向主文件夹 config/error_log.json
- **v0.15**：新增 tools/upload_presets.py 批量上传工具——自动扫描 presets/official/ 并上传到 GitHub
- **v0.15**：后处理脚本完善——presets/official/ 复制文件，presets/user/ 和 save/ 只建空目录
- **v0.15**：退出弹窗修复——隐藏 Cancel 按钮实现 X 关闭功能

## v0.14 — 预设系统增强

- **v0.14**：角色增益预设（character_buff）——新建预设类型，常驻+触发效果管理
- **v0.14**：分页式编辑弹窗——鸣链编辑改为 3 页：介绍 / 通用增益 / 特定增益
- **v0.14**：OCR 分类识别优化——提升分类准确率
- **v0.14**：deleteLater() 修复——防止退出时崩溃
- **v0.14**：使用预设搜索功能——按名称过滤预设列表
- **v0.14**：手册内容更新

## v0.12 ~ v0.13 — 预设系统改造

- **v0.12**：预设系统乘区化改造——预设数据结构与 damage_calc.py 统一
- **v0.12**：预设使用窗口重设计——来源选择（官方/用户）→ 分类卡片 → 列表
- **v0.12**：工具栏使用手册——内置使用说明书
- **v0.12**：全局禁用滚轮——防止意外修改数值
- **v0.13**：角色预设独立计算引擎——ResultPage + ResultListPage
- **v0.13**：预设命名/重命名、预设搜索

## v0.9 ~ v0.11 — 初始开发

- **v0.9**：初始开发，基础 GUI 框架搭建
- **v0.9**：伤害计算引擎（damage_calc.py）纯函数模块
- **v0.9**：暗色/亮色双主题系统（theme_system.py）
- **v0.9**：OCR 识别系统（ocr_engine.py）
- **v0.9**：存档系统（save/load）
- **v0.9**：预设系统框架（preset_manager.py / preset_builder.py / preset_loader.py）
- **v0.9**：共鸣链增益系统
- **v0.9**：关键词关联系统
- **v0.9**：自动化测试（pytest，186 用例）
- **v0.9**：Git 版本管理
- **v0.10**：数据流调试器（DataFlowViewerDialog）
- **v0.10**：关键词标签显示
- **v0.10**：process HTML 提取
- **v0.10**：自动按钮统一
- **v0.11**：主工具栏全局自动按钮同步
- **v0.11**：联动增益规则
- **v0.11**：共鸣链介绍页

## 自动化测试（pytest）

项目引入基于 pytest 的自动化测试体系，包含 **186 个测试用例**，全部 **2.9 秒内完成**。

**测试架构**：四层架构，damage_calc.py 是唯一真相来源

```
damage_calc.py  ← 唯一真相来源（纯函数，零 GUI 依赖）
    │               所有公式、常量、筛选逻辑都在这里
    │
    ├── import ── WWDmgCalc.py          主程序 GUI
    │              _matches_filter() → damage_calc.matches_filter()
    │              EnemyDefensePage.recalc() → damage_calc.calc_defense_zone()
    │              EnemyResistancePage._recalc() → damage_calc.calc_resistance_zone()
    │
    └── import ── tests/test_damage_formula.py  测试
                  from damage_calc import calc_defense_zone, ...
                  assert calc_defense_zone(90, 100, 0) == ...
```

**测试覆盖**：

| 测试类 | 用例数 | 覆盖内容 |
|--------|:------:|----------|
| TestDefenseZone | 10 | 敌方防御减伤（等级/无视/截断/范围） |
| TestResistanceZone | 7 | 抗性乘区（正负抗性/提升/减免/clamp） |
| TestIndepZone | 5 | 独立乘区（组内加法×组间乘法） |
| TestBaseZone | 7 | 基础乘区（攻/生/防，百分比+固定值） |
| TestBonusZone | 3 | 加成乘区（1 + Σ加成/100） |
| TestDeepenZone | 2 | 加深乘区 |
| TestCritZone | 5 | 暴击乘区+暴击率（150%底/5%底） |
| TestMultZone | 5 | 倍率乘区（增加+增幅） |
| TestFullDamageFormula | 3 | 完整伤害端到端 |
| TestFilterMatching | 10 | 元素/技能/效应筛选匹配 |
| TestGameConstants | 7 | 常量完整性 |
| TestEdgeCases | 4 | 边界条件（负数/零值/多组） |
| TestSaveFormat / Roundtrip / Backward | 14 | 存档结构验证、JSON 读写、兼容 |
| TestSyncBasics / SubName / Incremental | 7 | 共鸣链同步链路 |
| TestClassifyItemCategory | 46 | 词条名称→乘区分类路由 |
| TestCharBaseFlow / CombinedEntry / Echo | 22 | 各来源数据流端到端 |
| TestMixedSources / TestSourceToZoneCoverage | 23 | 多来源混合 + 全覆盖验证 |
| **合计** | **186** | |

## Git 版本管理

项目使用 Git 管理版本。关键操作：

```bash
# 日常存档（每次改完代码做一次）
git add .
git commit -m "改动说明"

# 出问题回退
git log --oneline          # 查看历史
git reset --hard <commit>  # 回退到指定版本
git push --force           # 强制推送（仅在需要时）
```

**被 .gitignore 忽略的文件**：

- `build/` `dist/` — 打包产物
- `__pycache__/` `*.pyc` — 缓存
- `.venv/` — 虚拟环境
- `.vscode/` `.idea/` — IDE 配置
- `config/error_log.json` — 错误日志
- `.pytest_cache/` `.claude/` `.codegraph/` — 缓存
- `ico/Wico*.ico` — 图标中间产物
- `presets/user/` — 用户预设
- `save/` — 用户存档
- `models/` — OCR 模型文件（过大不上传）
- `tools/.github_token` — GitHub Token（敏感文件）

---

## v1.0 补充修复（2026-06-12 ~ 2026-06-13）

- **修复**：`git_proxy_manager.py` TestThread 从方法内嵌类移到模块级，消除 Qt 信号警告
- **修复**：`git_proxy_manager.py` sock 和 socket 导入报黄
- **修复**：`git_proxy_manager.py` SOCKS5 测试改用 `git ls-remote`（urllib 不支持 SOCKS5），测试连接功能正常
- **验证**：socks5://127.0.0.1:15715 配给 git 后成功连接 GitHub
- **修复**：使用手册图片路径统一为 `manual/images/`，插入图片按钮也使用相同相对路径
- **修复**：after_build.py 打包规则——save/ 仅空目录、presets/user/ 仅空子目录，不打包开发机测试数据

- **v1.0**：预设作者系统 — 四种预设窗口新增「作者」输入框，JSON 存储 author 字段（2026-06-13）
- **v1.0**：贡献者名单弹窗 — 欢迎页按钮，按作者分组显示贡献内容，暗色主题（2026-06-13）
- **v1.0**：CONTRIBUTORS.md — 项目根目录贡献者名单，同步官方预设后自动重建（2026-06-13）
- **v1.0**：预设上传工具全面升级 — PR 审核模式（用户只需 GitHub 用户名无需 Token）、暗色主题重设计、保存 Token 前弹窗确认、贡献者名单自动追加（2026-06-13）
- **v1.0**：GitHub 分支保护 — main 开启 PR 审核 + 禁止 force push + 禁止删除（2026-06-13）
- **v1.0**：贡献者名单按钮 — 双主题样式（contribButton）避免暗色下不可见（2026-06-13）
- **v1.0**：贡献者双源读取 — 弹窗同时解析预设 JSON author + CONTRIBUTORS.md，已有记录不遗漏（2026-06-13）
- **v1.0**：上传工具注入 author — 投稿时 GitHub 用户名写入 JSON author 字段，弹窗和名单双轨同步（2026-06-13）
- **fix**：list_presets 中 author 读取放入 try 块内，防止 NameError（2026-06-13）
- **fix**：CONTRIBUTORS.md 路径修正 — 从 presets/ 改为项目主文件夹，启动时自动重建（2026-06-13）
- **fix**：恢复 CONTRIBUTORS.md 中被 rebase 覆盖的贡献者记录（JadeBrookYuyanxi，2026-06-13）
- **fix**：rebuild_contributors_md 空记录时不覆盖已有 CONTRIBUTORS.md（2026-06-13）
- **fix**：弹窗 CONTRIBUTORS.md 路径适配 frozen 打包模式（2026-06-13）
- **fix**：after_build.py 复制 CONTRIBUTORS.md 到发布包（2026-06-13）
- **fix**：移除启动时 rebuild 调用（会覆盖贡献者记录）（2026-06-13）
- **fix**：上传工具加 manifest.json 更新步骤 — 投稿时自动合并新文件，同步按钮能拉到新预设（2026-06-13）
- **fix**：author 注入顺序 — 从上传后移到上传前，确保 JSON 含 author（2026-06-13）
- **fix**：弹窗静默异常改为 traceback.print_exc，便于排查（2026-06-13）
- **fix**：CONTRIBUTORS.md 纠错 — 多次 PR 覆盖导致记录丢失，改为单行合并格式（2026-06-13）
- **fix**：同步 manifest.json 加缓存穿透参数 ?_=timestamp — CDN 强制回源，永远拉取最新（2026-06-13）
- **fix**：上传工具 CONTRIBUTORS.md 累积合并 — 同贡献者多次投稿合并为一行不覆盖（2026-06-13）
- **style**：预设构建器 4 个窗口隐藏「作者」栏（上传工具已自动处理）（2026-06-13）
- **整理**：清理 4 个测试预设文件 + CONTRIBUTORS.md / manifest.json 归零为正式版就绪状态（2026-06-13）

- **feat**：预设名称保存时校验 Win 系统 9 个非法字符（2026-06-13）
- **fix**：声骸预设显示名 / 改为 +，避免文件名冲突（2026-06-13）

- **经验**：manifest.json 冲突时应先检查远程，可能有他人提交的新预设，不要盲目以本地为准覆盖。正确做法：git pull → 以远程为准 → 扫描磁盘重建（2026-06-14）

- **feat**：应用声骸预设时弹窗勾选阶段效果 — 每个阶段可选，实时显示已选/5件，超标灰掉OK按钮（2026-06-14）
- **fix**：编辑已有声骸预设时 load_data 加 blockSignals，修复1件套预设打开出现多余卡片（2026-06-14）
- **style**：贡献者名单窗口加大20%/10% + 内容自动换行（2026-06-14）
- **fix**：贡献者名单去重 — CONTRIBUTORS.md条目格式标准化匹配JSON author格式（2026-06-14）
- **feat**：使用预设窗口改为双源混合浏览 — 跳过来源选择直接进列表，加 [官]/[我] 标签、全部/官方/个人三选过滤（2026-06-14）
- **feat**：声骸应用改为累积激活制 — 件数分配代替阶段勾选，required_count <= 件数自动激活（2026-06-14）
- **style**：分类按钮选中时蓝色边框高亮提示（2026-06-14）
- **fix**：echo_set 编辑已有预设 load_data 时 blockSignals 防信号冲突（2026-06-14）
- **fix**：同步官方预设后自动清理不在 manifest 中的本地旧文件 — 防止改名/删除残留（2026-06-14）
- **fix**：声骸预设 sync 后本地旧文件名自动清除，manifest 为唯一真相来源（2026-06-14）
- **fix**：倍率OCR合跨行公式 + 单字中文字碎片拼回上一行 — 修复丽贝卡双行倍率只识别到3条的问题（2026-06-14）
- **fix**：OCR 暴击→重击纠正仅限无公式行 — 子分类指示行纠正，有%公式的行保留原名，兼容未来暴击倍率（2026-06-14）

- **fix**：OCR碎片合并修复 — 1-2字中文碎片（如"裂象"）被粘到技能分类行（如"共鸣技能"），导致变长后匹配不到 _SKILL_CATEGORIES，所有倍率的 skill/category 全变成(无)。修复：碎片合并前三重检查——目标行不含百分号、不是系统词、不是技能分类行（2026-06-14）
- **fix**：OCR暴击→重击纠正仅限无%公式的纯文字行——子分类指示行纠正，有公式的倍率行保留原名，兼容未来暴击倍率（2026-06-14）
- **fix**：prev_line 过滤治疗行+纯标点行 — 清除后非空才赋值，防止「。 」或「治疗」污染回溯名称导致 0 结果（2026-06-14）
- **fix**：_SKIP_KW 加「治疗」— 治疗行跳过不再成为 prev_line 候选（2026-06-14）

- **fix**：结果列表分类筛选后卡片索引错乱 — visible_items 的 i 传给了 _build_card，导致展开/删除操作错误卡片。改用 self._items.index(item) 获取真实索引（2026-06-14）
- **fix**：底部弹簧行数计算 — last_row 用 len(self._items) 而非 len(visible_items)，筛选后布局异常。改为可见列表长度（2026-06-14）
- **fix**：使用预设列表来源标签 — 来源过滤为"全部"时列表项不显示 [官]/[我]。改为始终追加（2026-06-14）
- **feat**：上传工具用户名实时验证 — 输完 600ms 自动调 GitHub 主页验证，绿/红提示，未验证不可提交（2026-06-14）
- **style**：上传工具窗口 560→760px，文件列表/日志高度对应放大（2026-06-14）
- **feat**：Token 状态行加副标题提示作者专用 + 用户名栏提示动态切换（验证通过/失败/网络不通）（2026-06-14）
- **chore**：官方预设 name 字段和文件名去掉 -官方预设 后缀 — 6 个文件统一清理，使用预设列表已有 [官] 标签无需重复标识（2026-06-14）
- **fix**：基础数值变化时空卡片不更新 — _make_char_cb 中 auto_compute → compute，recalc → _update_all，无视自动开关（16-06-14）
- **fix**：结果详细窗口计算过程实时同步 — _recalc_one 在更新 zones 后同步生成 process_html，_update_result_labels 缓存优先 + fallback 重建（2026-06-14）
- **style**：计算结果页、结果详情的 process_html 统一归口 — 切勿让计算过程框内的 HTML 被覆盖，始终走 _recalc_one 重新生成（2026-06-14）
- **style**：计算结果页、结果详情的 process_html 统一归口 — 切勿让计算过程框内的 HTML 被覆盖，始终走 _recalc_one 重新生成（2026-06-14）
- **fix**：基础数值覆盖提示事件全记录（2026-06-14）——

  **现象**：启用「基础数值调整」后，计算结果页的计算过程框顶部出现一条 QLabel 提示文字「▸ 已启用手动填写基础数值」，本意是提醒用户目前在用手动值。但 QLabel 是放在 _process_layout 最顶部的独立 Widget，导致 HTML 表格还没渲染时它就卡在第一行位置，形成「置顶」效果，视觉上割裂了计算过程表格。同时结果详细窗口里**完全没有**这条提示——因为 ResultDetailDialog 读的是卡片缓存的 `process_html`，而该缓存由 `_build_card_process_html` 生成，调用 `_render_process_html` 时漏传了 `base_override_active` 参数。

  **根因链**：
  1. 提示被分到三个独立的渲染路径，各自缺失或重复，从未对齐
  2. `ResultPage.compute()` → `_render_process_html(base_override_active=True)` 在 HTML 的 `<br>` 中内嵌提示 ✅，但同时又有一个 QLabel Widget 被加在 layout 顶部 ❌ 形成双份+置顶
  3. `ResultDetailDialog._rebuild_process_html()` 自己重新构建 HTML，读 `self._page._base_override_enabled` 埋 `<tr>` 行在基础数值行下面 ✅，但这只在缓存为空时执行
  4. `ResultListPage._build_card_process_html()` → `_render_process_html(is_light=False)` **未传** `base_override_active` ❌，导致缓存缺失提示

  **修复**：
  1. 移除 ResultPage 中的 QLabel Widget（`_base_override_hint`），提示完全改为 HTML 内嵌
  2. `_build_card_process_html` 补传 `base_override_active` + `computed_base_zone`，缓存中自带提示
  3. `_rebuild_process_html` 将 `<p>` 标签改为 `<tr>` 标签，让提示作为表格行紧接在「基础数值」行下面
  4. 最终效果：计算结果页和详情窗口的提示位置/格式完全一致，维护单一渲染路径
- **fix**：编辑官方预设后保存到 original/ 目录而非 user/ — 记录 _edit_preset_source，save_preset 传入正确 source，杜绝使用预设窗口出现一官一我重复条目（2026-06-14）
- **fix**：关键词倍率增加/倍率提升不生效 — 它们被误列入 BONUS_SUFFIX，当作加成乘区处理。修复：从 BONUS_SUFFIX 移除，在 _recalc_one / ResultPage.compute 中显式从 keyword_assoc 注入，分别累加到 mult_inc（相加）和 mult_boosts（独立乘算）（2026-06-14）
- **chore**：漂泊者-气动官方预设覆盖更新 — user/ 副本移至 official/，含最新关键词关联配置（2026-06-14）

---

## v1.0 倍率系统重构（2026-06-14 ~ 2026-06-15）

### 倍率设置表格全面升级
- **refactor**：倍率设置改为关键词关联驱动的动态列表 — 移除固定 mult_boosts、_orig_ 逻辑、kw_mult_inc2 hack（2026-06-14）
- **fix**：动态倍率列表增加关键词匹配过滤，仅显示与当前卡关键词匹配的条目（2026-06-14）
- **fix**：清理 kw_mult_boost2 遗留引用 + _build_process 用 gathered data（2026-06-14）
- **refactor**：倍率设置表格完全照搬关键词关联界面8列结构 — 名称/副名称/序列号/数值/取值/来源/关键词关联/操作（2026-06-15）

### 交互增强
- **feat**：倍率表格添加折叠展开按钮（▼/▶），可收起腾出空间（2026-06-15）
- **style**：折叠按钮透明主题样式 — 无边框/透明背景/选中加粗（2026-06-15）
- **style**：折叠按钮加 ▼/▶ 箭头 + 左灰边框，明确可交互（2026-06-15）
- **style**：去掉折叠按钮左侧蓝/灰色竖杠，只保留加粗提示（2026-06-15）
- **fix**：倍率设置框架增高600 + 表格最小高度150可存3行（2026-06-15）
- **fix**：移除倍率设置固定高度600，折叠后框架自适应缩小（2026-06-15）

### 副名称互通
- **fix**：倍率表格副名称列补上 … 展开编辑按钮（2026-06-15）
- **fix**：副名称在 _make_sub_name_cell 之后再设文本，防止 reparent 丢失（2026-06-15）
- **feat**：倍率表格副名称编辑实时映射回关键词关联对应行（2026-06-15）

### 来源跳转 + 隐藏按钮
- **feat**：倍率表格来源按钮→跳转关键词关联+渐变动画 / 操作→隐藏按钮（2026-06-15）
- **fix**：隐藏条目保留在表格中（显示「隐藏中」），仅从计算排除（2026-06-15）
- **fix**：隐藏按钮点击后实时刷新表格+重算，auto_compute/_on_mult_changed 自动适配（2026-06-15）
- **fix**：来源按钮跳转关键词关联 — 修复 MainScreen 查找 + 平滑滚动+黄色叠层动画（2026-06-15）

### 数据流调试器
- **feat**：数据流调试器加「关键词关联」列，列数 5→6（2026-06-15）

### 上传工具
- **fix**：上传工具 _save_token 误复制 contributor 验证代码 → 删除，消除报黄（2026-06-15）
- **feat**：上传工具加 GitHub 用户名验证 — 访问主页 404 检测，无 API 限流（2026-06-15）

### 倍率系统修复（2026-06-15）
- **fix**：预设应用/保存崩溃 — preset_builder/preset_manager 移除已删除的 mult_increase/mult_boosts spinbox 引用（2026-06-15）
- **fix**：_recalc_one 关键词注入后提取倍率增加/提升到倍率乘区，解决结果列表卡片不关联问题（2026-06-15）
- **fix**：详情窗口倍率表格始终为空 — _sync_mult_entries 用 self._item 关键词 + 新建/复用对话框均调用（2026-06-15）
- **fix**：_gather_mult_data 补回 HIDDEN_ITEMS 过滤 — 隐藏按钮在详情窗口生效（2026-06-15）
- **style**：隐藏按钮用 stretch 居中容器包裹 — 对齐操作表头（2026-06-15）
